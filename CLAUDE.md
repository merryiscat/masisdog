# CLAUDE.md

> 상위 `C:\Users\minhy\project\CLAUDE.md`(언어=한국어, 패키지=uv, Git 컨벤션 등)를 상속한다.
> 여기엔 masisdog 고유 사항만 적는다.

## 이 프로젝트는

**에이전트-퍼스트 폰**(masisdog) — 온디바이스 LLM 에이전트가 안드로이드 폰의 관리자로 상주하며
저장공간·네트워크·알림·화면을 관장하고, 재부팅 후에도 스스로 권한을 복구하며 생존한다. 개인용.

**핵심 결정**: 커스텀 OS가 아니라 **순정 안드로이드 앱 계층**으로 시작한다.
(갤럭시 One UI 8부터 부트로더 언락 차단 → 커스텀 OS 불가. 앱 계층으로 검증하고 천장 닿으면 Pixel/OnePlus로 기기 이전, 앱 코드는 이식됨.)

## 문서 인덱스 (docs 기반 관리)

| 파일 | 내용 | 참조 시점 |
|------|------|----------|
| `docs/plan.md` | 전체 기획 — 경로 결정 근거, 능력/천장, Shizuku 부활 체인, 폰 조작 스택, 마일스톤, 기기 전략 | 설계·의사결정 시 |

> 새 기획/스펙/TODO 문서가 생기면 이 인덱스에 반드시 추가할 것.

## 현재 단계

**1단계 = PC에서 폰 원격 조작으로 개념 검증** (`mobile-mcp` / `uiautomator2` / `adb`).
검증된 로직을 2단계에서 온디바이스 앱(Accessibility Service)으로 이식한다.
마일스톤은 `docs/plan.md` §6.

## 작업 시 주의

- Python은 uv (`uv add` / `uv run`). 온디바이스 LLM 모델(*.gguf 등)은 `models/`에 두고 **커밋 금지**.
- 폰 조작 실험 산출물(로그·캡처)은 `logs/`, `captures/`로 — gitignore 처리됨.
- 안드로이드/LLM 관련 라이브러리·SDK를 다룰 땐 context7로 최신 문서 확인.
